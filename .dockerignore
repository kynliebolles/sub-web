# 版本控制 / CI
.git
.gitignore
.github

# 依赖与构建产物（镜像内会重新安装/构建）
node_modules
dist

# 本地文档与自建服务编排（前端构建不需要）
docs
services

# 日志与系统文件
*.log
npm-debug.log*
yarn-error.log*
.DS_Store

# 注意：.env 是 Vite 构建时变量来源，必须保留，不要忽略
